---
title: Authenticate Notebooks
sidebar_label: Authenticate Notebooks
path: basics/tutorials/login/login
nb_path: basics/tutorials/login.ipynb
github: 
  https://github.com/fusedio/fused-docs/blob/main/docs/basics/tutorials/login.ipynb
colab: 
  https://githubtocolab.com/fusedio/fused-docs/blob/main/docs/basics/tutorials/login.ipynb
description: Tutorial on how call UDFs asynchronously with Fused.
---

import LinkButtons from "@site/src/components/LinkButtons.jsx";
import CellOutput from "@site/src/components/CellOutput.jsx";
import {BokehFigure, PlotlyFigure} from "@site/src/components/Plotting.jsx";
import Tag from '@site/src/components/Tag'

## <Tag color="#D1E550" fontColor="#141414" >🚧 Under Construction</Tag>

To use the Fused Python SDK within a Python environment, you'll need to authenticate to
interact with the API and run UDFs.

```python
# !pip install fused -q
```

When running code on a local machine, authentication will happen automatically whenever
you start calling fused functions. If no credentials have already been saved to disk,
when you run import fused, you'll see a message describing how to login.

If credentials haven't yet been saved to your local machine (in `~/.fused/credentials`),
this will open up your web browser so you can log in. After the first login, credentials
will be saved and automatically loaded as needed.

The second line will print out a link for you to click on. That link will navigate you
to our login page. After logging in, you'll see

```python
from fused import NotebookCredentials

credentials = NotebookCredentials()
```

```python
# Paste the credentials snippet found in the URL above
credentials.finalize(code="...")
```

Now the api object will have access to the Fused API.

Note: When running code on a remote machine, the above command-line method won't work
because the remote command line is unable to open a web browser. For those cases, you
can copy the `~/.fused/credentials` file to the target system.

```python

```

