---
id: local-files
title: Local Files
sidebar_label: Local Files
sidebar_position: 1
---

# Local Files

Quickly bring any local data into Fused.

## Drag & Drop

Drop files directly into the [File Explorer](/workbench/file-explorer/):

![Drag and drop files directly into Workbench](/img/tutorials/drag_and_drop.gif)

## Upload with Python

[Install `fused`](/python-sdk/#python-install), [authenticate](/python-sdk/authentication/#authenticate) & run:

```python
fused.api.upload("my_local_file.csv", "fd://my_data/file.csv")
```

:::note
[`fd://`](/guide/advanced-setup/file-system#fd-s3-bucket) is the Fused provisioned private S3 path for your team.
:::

## Optimize Data Loading

**For files &lt; 1GB:** Use [caching](/guide/working-with-udfs/caching/) to speed up loading:

```python
@fused.udf
def udf(path: str = "s3://fused-sample/demo_data/housing_2024.csv"):
    import pandas as pd
    
    @fused.cache
    def load_data(path):
        return pd.read_csv(path)

    return load_data(path)
```

This is especially useful for slow formats (CSV, Excel, etc.).

**For files &gt; 1GB:** Use [`fused.ingest()`](/python-sdk/top-level-functions/#fusedingest) to create cloud-optimized, partitioned files:

```python
job = fused.ingest(
    input="https://www2.census.gov/geo/tiger/TIGER_RD18/LAYER/TRACT/tl_rd22_11_tract.zip",
    output="fd://census/dc_tract/",
)

job.run_batch()
```
