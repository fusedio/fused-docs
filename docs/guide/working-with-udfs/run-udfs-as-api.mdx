---
id: run-udfs-as-api
title: UDFs as API
sidebar_label: UDFs as API
sidebar_position: 2
---

# UDFs as API

Every UDF can be used as a file system:

![Dynamic File System Overview](/img/core-concepts/dynamic_file_system.png)

## Output formats

UDFs can return data in various formats via the `format` query parameter:

### Vector data formats

| Format | Extension | Description |
|--------|-----------|-------------|
| `parquet` | `.parquet` | Efficient columnar storage format |
| `geojson` | `.geojson` | Standard GeoJSON format |
| `json` | `.json` | JSON objects and GeoJSON |
| `csv` | `.csv` | Comma-separated values |
| `mvt` | `.mvt` | Mapbox Vector Tiles |
| `feather` | `.feather` | Apache Arrow IPC format |
| `excel` | `.xlsx` | Excel spreadsheet |
| `xml` | `.xml` | XML data |
| `html` | `.html` | HTML document |

### Raster data formats

| Format | Extension | Description |
|--------|-----------|-------------|
| `png` | `.png` | Lossless raster images |
| `jpeg` | `.jpeg` / `.jpg` | Compressed raster images |
| `gif` | `.gif` | Animated or static images |
| `webp` | `.webp` | Modern efficient image format |
| `apng` | `.apng` | Animated PNG images |
| `svg` | `.svg` | Scalable vector graphics |
| `tiff` | `.tiff` / `.tif` | GeoTIFF and TIFF rasters |
| `npy` | `.npy` | NumPy array binary format |

## Example usage

### Returning as JSON

```bash
https://udf.ai/YOUR_TOKEN.json
```

### Returning as CSV

```bash
https://udf.ai/YOUR_TOKEN.csv
```

### Returning as HTML

```bash
https://udf.ai/YOUR_TOKEN.html
```

## Tile endpoints

For map tile servers, use the `tiles` path with `{z}/{x}/{y}` template:

### Vector tiles (MVT)

```bash
https://udf.ai/YOUR_TOKEN/run/tiles/{z}/{x}/{y}?=mvt
```

:::note Layer Name
When consuming Fused MVT tiles in tools that require a `source-layer` name (like Mapbox GL JS, MapLibre, or react-map-gl), the layer is always named `udf`.
:::

### Raster tiles (PNG)

```bash
https://udf.ai/YOUR_TOKEN/run/tiles/{z}/{x}/{y}?=png
```

## Integrations

These dynamic endpoints work with:
- [DeckGL](/guide/data-input-outputs/export-api/geospatial-export#deckgl) - Interactive maps
- [Mapbox](/guide/data-input-outputs/export-api/geospatial-export#mapbox) - Web maps
- [QGIS](/guide/data-input-outputs/export-api/geospatial-export#qgis) - Desktop GIS
- [Felt](/guide/data-input-outputs/export-api/geospatial-export#felt) - Collaborative mapping
- Google Sheets - Spreadsheet import
