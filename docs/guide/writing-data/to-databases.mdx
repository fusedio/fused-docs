---
title: To Databases
sidebar_label: To Databases
sidebar_position: 3
description: Write data to Snowflake, BigQuery, PostgreSQL and other databases
---

# Writing to Databases

Fused UDFs can push data directly to data warehouses and databases.

---

## How It Works

1. **Store credentials** in Fused Secrets
2. **Connect** using standard Python database libraries
3. **Write** using DataFrame methods or SQL

---

## Supported Databases

| Database | Write Method | Snippets |
|----------|--------------|----------|
| **Snowflake** | `write_pandas()` | [→ Code](/reference/data-loading/databases#snowflake) |
| **BigQuery** | `load_table_from_dataframe()` | [→ Code](/reference/data-loading/databases#bigquery) |
| **PostgreSQL** | `to_sql()` or `to_postgis()` | [→ Code](/reference/data-loading/databases#postgresql--postgis) |
| **Redshift** | `to_sql()` | [→ Code](/reference/data-loading/databases#snowflake) |

---

## Credentials Setup

Add database credentials in **Workbench → Preferences → Secrets**:

- `SNOWFLAKE_USER`, `SNOWFLAKE_PASSWORD`, `SNOWFLAKE_ACCOUNT`
- `POSTGRES_URL`
- etc.

See [Secrets Management](/reference/workbench/preferences#secrets-management).

---

## Append vs Replace

Most write methods support `if_exists` parameter:

```python
# Append to existing table
df.to_sql("table", engine, if_exists="append")

# Replace table
df.to_sql("table", engine, if_exists="replace")
```

---

## Code Examples

- [Snowflake write snippets](/reference/data-loading/databases#snowflake)
- [BigQuery write snippets](/reference/data-loading/databases#bigquery)
- [PostgreSQL/PostGIS write snippets](/reference/data-loading/databases#postgresql--postgis)
