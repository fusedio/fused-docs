---
title: Data Loading Reference
sidebar_label: Data Loading
sidebar_position: 3
description: Quick reference for loading all data sources in Fused
---

# Data Loading Reference

Copy-paste code snippets for loading data from any source.

## Quick Links

| Source Type | Page |
|-------------|------|
| CSV, Parquet, GeoJSON, COG | [Files](/reference/data-loading/files) |
| S3, GCS, Azure, HTTP | [Cloud](/reference/data-loading/cloud) |
| Snowflake, BigQuery, Postgres | [Databases](/reference/data-loading/databases) |
| STAC, GEE, Overture, APIs | [Specialized](/reference/data-loading/specialized) |

---

## Most Common

### Read Parquet

```python
import pandas as pd
df = pd.read_parquet("s3://bucket/file.parquet")
```

### Read GeoJSON

```python
import geopandas as gpd
gdf = gpd.read_file("s3://bucket/file.geojson")
```

### Read COG

```python
common = fused.load("https://github.com/fusedio/udfs/tree/main/public/common/")
arr, color_map = common.read_tiff(bounds, "s3://bucket/file.tif")
```

### Query with DuckDB

```python
import duckdb
conn = duckdb.connect()
df = conn.execute("SELECT * FROM 's3://bucket/file.parquet' LIMIT 100").df()
```

---

For detailed guides, see [Loading Data Guide](/guide/loading-data/).
