---
id: versions
title: Versioning
tags: [code, workbench, udf, builder]
sidebar_position: 6
---

import Tag from '@site/src/components/Tag'

<Tag color="#3399ff">Developer, Professional and Enterprise</Tag> _Versioning with GitHub is accessible to organizations with a Fused Developer, Professional & Enterprise subscription (see [Plans](https://www.fused.io/pricing))._

Versioning in Fused is built on top of [Canvas Checkpoints](/workbench/canvas-checkpoints). A checkpoint captures the state of your Canvas at a point in time — versioning lets you push that state to GitHub so your team can reuse and collaborate on it.

You'll need to [connect your GitHub repo](/guide/advanced-setup/git-integration/#configuring-github-integration) first.

## Versioning a Canvas

### Pushing changes

Push your Canvas state to GitHub by creating a PR from a checkpoint.

1. Create a [checkpoint](/workbench/canvas-checkpoints) of your Canvas
2. Click **Create a PR** on the checkpoint
3. (Optional) Add a commit message or change the target repository
4. Click **Open PR**
5. Merge the PR on GitHub

![Creating a PR from a checkpoint](/img/workbench/canvas/push_changes_pr.png)

:::tip Changing the repository
If the folder name and repository are locked (because the canvas already has saved Git details), click the pencil icon to unlock them. This lets you push the UDF to a different repository — for example, to a public repo instead of your team's private one.

![Edit locked folder name and repository](/img/workbench/canvas/edit_repo_locked.png)
:::

Once merged, everyone on your team can pull the Canvas from the connected repository.

### Pulling changes

Pull the latest Canvas state from GitHub to stay in sync with your team.

1. Open the Canvas checkpoints panel
2. Click **Clone from GitHub**
3. Select the checkpoint to restore

![Pulling from GitHub](/img/workbench/canvas/pull_changes_pr.png)

This replaces your current Canvas state with the version from GitHub.

## Versioning a UDF

You can also commit individual UDFs to GitHub directly from the Canvas.

### Committing a UDF

1. Click **Versions** in the three-dot menu
2. (Optional) Add a commit message or change the target repository
3. Select the UDF to commit
4. Click **Open PR**
5. Merge the PR on GitHub

![Version Control Flow](/img/tutorials/version_control_flow.png)

UDFs merged to GitHub are available in your Team UDFs in the [UDF Explorer](/workbench/udf-explorer).
